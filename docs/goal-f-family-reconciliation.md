# Goal F requirement-family reconciliation

Current status: Goal F is deployed at schema 19; live acceptance, recreation, retained Goal-E rollback and return pass. [Release evidence](goal-f-release-evidence-20260922.md) supersedes pending-release statements in the dated checkpoint history below. Native provider activation remains blocked.

## Studio/editor family

The f445c5d source is inspected at its real mutation boundaries: `requireAdmin` gates global Studio and its editor actions; record-specific actions save/delete pages, pieces and Process notes with dependency guards and route invalidation. `inline-edit-registry.ts` admits typed declared fields/modes only. Site structure/navigation/home service/feature lists use structured controls and canonical settings versions. The persisted commission-type labor/markup fields do exist; their estimator consumption was a genuine defect, corrected and independently validated below.

Current isolated `inline-edit-registry`, `studio-mutations`, and `studio-settings-autosave` tests pass 50/50. They exercise whitelist validation, list edits, intentional empty values, SQLite rollback/reference synchronization, durable mutation IDs/replay, conflict and retry handling, versions and autosave. Retained exact v19 authenticated mutation/rendering evidence covers ordinary record editors; current Goal-F 22-group seller fixture separately proves server isolation and canonical media crop/assignment/order/publication behavior. No production record was edited for this family audit. Final candidate recreation remains explicitly open in W2-02.22 and release rows.

## Estimator, capacity, paid-order metric and category editing

The estimator previously ignored saved commission-type labor/markup values. Both the public preview and server recalculation now consume that persisted policy. Explicit zero is valid; invalid values use deterministic defaults. Literal W2/W3 #8 requests a labor/material/markup estimate; the matrix phrase “editable labor rates/hours” overstated it. The hourly planning rate remains $75, while base hours and markup are editable. Material figures remain planning estimates, not live supplier quotations. Buyer budget is stored independently of the server-calculated total.

Public capacity now considers the primary business's active projects only: archived, cancelled and completed projects do not inflate hours or queue size, and unrelated businesses do not alter William's estimate. Studio deliberately requests an all-business aggregate. The overview's paid-order value includes only paid, non-refunded/non-cancelled orders created during the current UTC month; its label states this metric precisely. This is not payment-settlement accounting.

The category workspace and new-category form use file import/preview/built-in selection instead of exposing raw SVG markup. The existing sanitizer still rejects active content, external references, unsupported attributes and malformed SVG. No schema or dependency change is part of this packet.

Current packet checks: estimator plus category tests **11/11 pass**; typecheck passes; changed-source ESLint passes (the existing visualizer raw-image advisory remains). Chromium and Firefox each pass saved pricing → reload → public calculation equality, existing SVG import → reload, new category SVG import → reload, and raw-code-editor absence. There are zero page/console errors. Early harness input before hydration was corrected by waiting for hydration/network idle and actual mutation responses; it was not an application defect. Evidence is restricted outside Git: `reconcile-final-focused.log`, `reconcile-final-typecheck.log`, `reconcile-category-lint.log`, `reconcile-lint.log`, `reconcile-browser-report.json`.

Only isolated synthetic data was edited. The disposable port-3334 service is stopped; generated Next development type-path drift is removed. Production remains Goal E/schema 16. Changed public behavior retains explicit final-candidate/live actions in the matrix. Full final-source/build/release gates have not been run for this source.

## Account, project and profile family

Concrete contradictory source evidence required an account correction: signup issued a session before verification, and general account/project/cart reads trusted that email. General `getCurrentUser` now requires verified customer email; a separate profile-only reader allows verification notices and editing the pending account's own profile, without linked projects. The project authorization helper also checks verification independently. Staff role/business authorization remains unchanged. Guest project capabilities continue to work. Login return targets are restricted to local paths. Password reset rejects missing expiry and password changes revoke old sessions in the same transaction as token consumption.

Profile controls previously discarded photo-removal and gradient choices; these now persist with typed color validation while preserving unrelated metadata. Imported profile photos previously resolved to static `/profiles` paths and returned 404. Only the two bundled SVG portraits retain that static mapping; real uploads use `/media`. Both browser engines now prove actual HTTP 200 image delivery, blob preview and revocation, upload/save/reload, gradient persistence, and photo removal. Project overview cards now link to the authorized project tracker. The legacy account checkout-return notice no longer claims payment receipt from URL parameters.

Current narrow evidence: **20/20** account/token/project/commission/cart/media tests pass; changed-source typecheck and ESLint pass. Chromium and Firefox each prove pending account profile-only access, project/cart isolation, denied private-media 404 with no-store, verified own-project/media access, unrelated-project denial, truthful forged-checkout return copy, and avatar flows. Zero unexpected page/console errors. Evidence outside Git: `reconcile-account-final-tests.log`, `reconcile-account-typecheck.log`, `reconcile-account-lint.log`, `reconcile-account-browser.json`. These tests use the existing synthetic seller fixture, with all providers disabled; no real account or customer message is involved.

Unchanged project stages, typed status autosave, public/private timeline and correspondence reuse exact v19 and B2 release evidence after current source inspection. New authorization/profile corrections stay ACTIVE only for their final candidate/live proof. No schema/dependency changes or production mutation occurred. Full final source/release gates remain outstanding.

## Media, search and metadata family

Current inspection connects browser photo/video uploads to bounded permanent storage, indexing and optional assignment; new files receive deterministic folder/date groups. Local sidecar clustering/training is actually proved by the retained Goal-F operational packet. Proposals do not publish or override manual identity. Source-folder previews require explicit application. Startup/upload/explicit refresh discovers external files; this is not a continuous filesystem watcher claim.

The full Media library and pickers reuse the exact dense three-page, keyboard, focus, save/reload, rename/delete and read-only GET evidence from v19/Goal C. Transactional media operations, private flags, assignment/order/crop and rename preservation have current Goal-F 80-test and two-engine seller proof. Goal A's technical/editorial distinction remains intact. No new scan/recompute or source-original change was performed.

Search uses weighted Unicode FTS5 and transactional synchronization, with admin-only global private results and business-scoped seller search. Retained Goal-F FTS trigger/visibility/rollback/rebuild tests and actual sidecar rerank/fallback proof cover the implementation. Reference-image search remains honest browser-local color/material cue assistance; it does not claim piece recognition. Record/source timestamps and hashes remain provenance evidence rather than guessed identity.

The explicit single-writer SQLite concurrency boundary and database transition trigger are documented in the seller contract. Native commerce provider activation and final schema-19/live isolation remain separate obligations.

## Commerce semantics family

The existing 80-test checkpoint covers atomic stock reservations/releases, owned cart snapshots/quantity, coupon/subtotal/provider payload agreement, signed webhook validation, early callbacks, event replay/collision/out-of-order handling, durable invoice/label retry, concurrent purchase protection, account binding and reopen. These are authoritative application-semantic proofs, not native Stripe/EasyPost activation. The native provider clauses remain blocked with exact operator steps. Final schema-17/19 release acceptance remains active. No provider request, charge, label purchase or customer message was made during this reconciliation.

## Process preview, public content and brand

The old Process editor instructed users to view the public page after saving, but drafts correctly remained inaccessible there. A private preview now reads current form fields in existing/new administrator and seller editors without publishing. It renders the same marked/sanitized Markdown body, escaped title/excerpt and persisted media cover in a sandboxed frame; scripts, forms and top navigation are not enabled. `allow-same-origin` permits authenticated image requests without enabling scripts. Current draft fields can be refreshed or closed. There is no provider call or new dependency. Renderer API checked against [Marked documentation](https://marked.js.org/using_pro#renderer).

Current acceptance: one focused preview regression; typecheck/affected lint; six Chromium/Firefox groups covering existing admin draft, unsaved new admin note and seller draft. Preview formatting is visible, publication unchanged, public draft produces not-found/noindex (Next streamed response may carry HTTP 200), and 390/1440 layouts have no horizontal overflow. Zero console/page errors. Restricted evidence: `reconcile-process-tests.log`, `reconcile-process-typecheck.log`, `reconcile-process-lint.log`, `reconcile-process-browser.json`. New account/preview regressions are included in the canonical npm test command.

Current source and retained exact Goal-D/v19 proofs cover William's profile, honest compact avatar, piece sharing, owner social links, Markdown/source/cover authoring, shared brand icons/local font, header/search behavior, services/home structure, separate Process galleries, price policy, local-fulfillment privacy, guest capability/draft handling and actionable fallback pages. This does not close the known retired seeded Cooper/footer copy or the final release-wide browser/source obligations.

## Account rename and deletion safety

Last-admin protection previously existed in the structured autosave service but could be bypassed by the legacy save path. The database write boundary now validates roles and prevents deletion/demotion of the last administrator. Save/rename/delete wrap all account and dependent-reference changes in the existing nest-safe transaction helper. Account deletion removes account carts rather than converting them into guest carts.

Current evidence: 55/55 account, seller-workspace and Studio mutation tests; typecheck and affected ESLint pass. The added regression injects a reference-update failure, proves full rollback, then proves successful rename preserves identity, metadata and sessions across database reopen. Deletion revokes sessions/cart access and the sole administrator remains protected. The initial fixture contained two administrators and was corrected to exercise the actual last-administrator boundary. Restricted logs: `reconcile-account-rename-final-tests.log`, `reconcile-account-rename-typecheck.log`, `reconcile-account-rename-lint.log`. H-11 retains only final candidate/release validation. No new schema, dependency or production change.

## Final public-content inventory and release boundary

Read-only production inventory covers all persisted site settings, seven pages, 25 pieces, zero posts and two public profiles. Its only absolute external URL hostname is `github.com`. Cache-busted HTTPS requests to all 34 current public route variants return 200; URL attributes contain canonical `woodmat.ch`, expected Cloudflare challenge/Insights hosts, social share hosts and the already-known GitHub promotion. No retired canonical site hostname appears. Redirect/security checks are retained, not repeated. Restricted evidence: `reconcile-public-link-persisted.json` and `reconcile-public-link-live.json`. The legacy GitHub/Cooper default promotion remains an explicit schema-18 release obligation.

Inquiry forms/wrappers/context helpers, MediaCollection, structured footer/settings editors and shared CSS are byte-identical to accepted deployed source `a019ec6`. Their exact B1/B2/D/E/v19 proof remains applicable: About/Contact quick inquiries, separate detailed planner, contextual piece intent/source, separate per-piece gallery/build sequence, clean editorial geometry and structured footer controls. No production edit or broad browser rerun was needed.

Every remaining ACTIVE row now identifies its final-source, candidate or release obligation. Final tests/build/visual gates have not yet run at the frozen source, and no Goal-F production transition has begun. Provider-native activation remains blocked separately. Documentation rows require final identity/evidence updates after release; they cannot be marked complete from a pending-release manual.
